<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.winify.happy_hours"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="16"/>

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <application android:label="@string/app_name" android:icon="@drawable/winify">
        <activity android:name=".activities.service.LogInActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.MainActivity"/>
        <activity android:name=".activities.service.ShowUserInfoActivity"/>
        <activity android:name=".activities.service.CalendarActivity"/>
        <activity android:name=".activities.service.SettingsActivity"/>

        <service android:name=".activities.service.WifiService"></service>
    </application>

</manifest>
